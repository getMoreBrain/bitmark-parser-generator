/* eslint-disable @typescript-eslint/no-var-requires */
// const { bmg } = require('../dist/browser/bmg.min');

// import { bmgTests } from './bmg-test';

// // Run tests on dist (TS compiled, rollup bundled and minified file)
// bmgTests(bmg);

describe('bmg-dist', () => {
  describe('dummy', () => {
    test('test', async () => {
      //
    });
  });
});
