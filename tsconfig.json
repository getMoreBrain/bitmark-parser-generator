{
  "compilerOptions": {
    "noEmit": true,
    "target": "ESNext",
    "module": "NodeNext",
    // "lib": ["ESNext"],
    "strict": true,
    "allowJs": true,
    "checkJs": false,
    "jsx": "preserve",
    "rewriteRelativeImportExtensions": true,
    "erasableSyntaxOnly": true,
    "verbatimModuleSyntax": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "skipLibCheck": true,
    // Required for tsup until it supports TypeScript 6.0 properly
    "ignoreDeprecations": "6.0"
  },
  "include": ["**/*.ts"],
  "exclude": [
    "dist",
    "assets",
    "coverage",
    "docs",
    "submodules",
    // Catch-22: Have to exclude as it includes imports from the build
    // output so checks will fail if not built,
    "test/standard/web-bpg-minified-wrapper.js"
  ]
}
